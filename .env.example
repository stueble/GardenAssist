# Database connection
# SQLite (default): file:/data/gardenassist.db
# PostgreSQL:       postgresql://user:password@host:5432/gardenassist
DATABASE_URL=file:/data/gardenassist.db

# Directory for SQLite database file and uploaded attachments
DATA_DIR=/data

# Backend port (default: 3000)
PORT=3000
